FileTree fileTree = fileTree(rootDir) {
	include "modules/**/bnd.bnd"
	include "themes/**/liferay-theme.json"
}

URI rootURI = rootDir.toURI()

fileTree.each {
	File file ->

	String dirName = rootURI.relativize(file.parentFile.toURI())

	include ":" + dirName.replaceAll("[\\/]", ":")
}